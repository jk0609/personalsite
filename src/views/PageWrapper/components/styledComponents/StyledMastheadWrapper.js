import styled from 'styled-components';

export default styled.div`
  .masthead {
    height: 6em;
    padding-right: 4em;
    display: flex;
    justify-content: flex-end;
    align-items: center;
  }

  a {
    margin: 1em;

    :hover {
      border-color: #fff;
    }
  }
`;
